# 本地開發環境變數範例
# 複製此檔案為 .env.local 並根據您的本機設定修改
# .env.local 檔案不會被提交到版本控制

# API Base URL - 本機開發
# 使用 HTTPS（建議）
VITE_API_URL=https://localhost:7198/api/v1

# 或使用 HTTP
# VITE_API_URL=http://localhost:5063/api/v1

# 如果後端在不同機器或使用不同端口
# VITE_API_URL=http://192.168.1.100:5063/api/v1
